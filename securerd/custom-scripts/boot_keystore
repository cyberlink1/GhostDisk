#
# this is boot_keystore and sets up what the user is prompted for
#
boot_prompt() {
    local pass
    printf "Enter Boot Password: " >/dev/console
    stty -echo </dev/console
    read -r pass </dev/console
    stty echo </dev/console
    printf "\n" >/dev/console
    echo "$pass"
}
